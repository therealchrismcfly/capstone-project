import styled from 'styled-components';

const StyledWelcomeButton = styled.button`
	padding: 18px;
	border: none;
	border-radius: 25px;
	background-color: var(--spoiledEgg);
	font-size: 14px;
`;

export default StyledWelcomeButton;
