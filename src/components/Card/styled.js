import styled from 'styled-components';

const StyledCard = styled.li`
	position: relative;
	z-index: 10;
	width: 400px;
	height: 300px;
	margin-bottom: 220px;
	list-style: none;
`;

export default StyledCard;
